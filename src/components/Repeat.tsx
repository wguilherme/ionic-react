import React from 'react';

interface RepeatProps { }

const repeatComponent: React.FC<RepeatProps> = () => {
  return (
    <div className="container">
works
    </div>
  );
};

export default repeatComponent;
